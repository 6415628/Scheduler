Quintiq file version 2.0
#parent: #root
Response OnCreated () id:Response_FormTimeConstraintVisualization_OnCreated
{
  #keys: '[142956.3.495861835]'
  CanBindMultiple: false
  DefinitionID: 'Responsedef_WebComponent_OnCreated'
  GroupServerCalls: true
  QuillAction
  {
    Body:
    [*
      // Set data holder before any logic is called
      DataHolderCustomDrawConfiguration.Data( shadow( Schedule.CustomDrawConfiguration() ) );
      
      // Enable legend 'buttons; so they won't be grayed out.
      this.EnableLegendButtons();
    *]
  }
}
