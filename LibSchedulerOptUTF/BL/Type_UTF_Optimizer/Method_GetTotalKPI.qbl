Quintiq file version 2.0
#parent: #root
Method GetTotalKPI (Schedule schedule) as Real
{
  Description: 'Return the Total KPI of the puzzle which is used for checking if the start situation matches the benchmark Total KPI and can be used to check the result of the optimizer.'
  IsAbstract: true
  TextBody:
  [*
    LibUTF_Util::LogError_MethodShouldBeOverridden( this.Name(), this.DefinitionName() );
    
    return 0.0;
  *]
}
