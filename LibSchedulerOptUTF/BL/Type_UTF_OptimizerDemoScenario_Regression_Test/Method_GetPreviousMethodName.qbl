Quintiq file version 2.0
#parent: #root
Method GetPreviousMethodName () as String
{
  TextBody:
  [*
    // Jeroen Keijzer Jun-5-2020 (created)
    return guard( this.CurrentMethod().PreviousTestMethod().Name(), '' );
  *]
}
