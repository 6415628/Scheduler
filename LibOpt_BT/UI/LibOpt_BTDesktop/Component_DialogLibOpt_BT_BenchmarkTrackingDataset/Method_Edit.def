Quintiq file version 2.0
#parent: #root
Method Edit (GUIComponent parent) id:Method_DialogLibOpt_BT_BenchmarkTrackingDataset_Edit
{
  #keys: '[143208.0.123062017]'
  Body:
  [*
    data := shadow( LibOpt_BT_BenchmarkTrackingDataset );
    
    Dialog.Data( data );
    
    if( Dialog.DoModal( parent ) > 0 )
    {
      data.Commit();
    }
  *]
}
