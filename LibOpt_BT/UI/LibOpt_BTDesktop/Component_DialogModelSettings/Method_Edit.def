Quintiq file version 2.0
#parent: #root
Method Edit (GUIComponent parent, LibOpt_BT_TrackingTableBase selection) id:Method_DialogModelSettings_Edit
{
  #keys: '[139544.0.955528794]'
  Body:
  [*
    data := shadow( selection );
    
    Dialog.Data( data );
    
    if ( Dialog.DoModal( parent ) > 0 )
    {
      data.Commit();
    }
  *]
}
