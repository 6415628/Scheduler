Quintiq file version 2.0
#parent: #root
StaticMethod AddRealToBinaryValue (BinaryValue input, Real value) const as BinaryValue
{
  Description: 'Covers the BinaryValue to a RealVector, appends the value at the end of the vector and returns the new vector as BinaryValue'
  TextBody:
  [*
    // afn3 Oct-8-2019 (created)
    values := RealVector::Construct(  input );
    values.Append( value );
    return values.AsBinaryValue();
  *]
  InterfaceProperties { Accessibility: 'Module' }
}
