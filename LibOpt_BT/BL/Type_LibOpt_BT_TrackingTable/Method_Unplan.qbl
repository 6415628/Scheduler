Quintiq file version 2.0
#parent: #root
Method Unplan (LibOpt_Scope scope)
{
  Description: 'Unplans the current scope from the dataset'
  TextBody:
  [*
    // This needs to be overridden and implemented by the AE.
    // The best way is likely to define an unplan method on a different object and call that from here, if the way of doing this is the same for each optimizer / decision.
  *]
  InterfaceProperties { Accessibility: 'Extensible' }
}
