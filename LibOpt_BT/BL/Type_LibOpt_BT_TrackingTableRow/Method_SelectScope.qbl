Quintiq file version 2.0
#parent: #root
Method SelectScope (LibOpt_Optimizer optimizer, LibOpt_Run run) as LibOpt_Scope
{
  Description: 'To be implemented by the AE. Returns scope of the optimizer run given an imput percentage.'
  TextBody:
  [*
    // This needs to be overridden and implemented by the AE. 
    // The scope needs to be set on the Run here and it also needs to be returned.
    return run.StartScope();
  *]
  InterfaceProperties { Accessibility: 'Extensible' }
}
