Quintiq file version 2.0
#parent: #root
StaticMethod Create (LibOpt_BT_TrackingTable owner, String description, Real size) as LibOpt_BT_TrackingTableRow
{
  TextBody:
  [*
    owner.Row( relnew,
               Description := description,
               Size := size );
  *]
}
