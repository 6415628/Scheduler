Quintiq file version 2.0
#parent: #root
MethodOverride GetCells () as owning LibOpt_BT_TrackingTableCells
{
  TextBody:
  [*
    set := construct( LibOpt_BT_TrackingTableCells );
    set.Add( this );
    
    return & set;
  *]
}
