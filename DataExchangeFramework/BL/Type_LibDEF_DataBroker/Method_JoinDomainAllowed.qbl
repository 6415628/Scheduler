Quintiq file version 2.0
#parent: #root
Method JoinDomainAllowed () const declarative remote as Boolean
{
  Description: 'Precondition: Check if join domain is allowed.'
  TextBody:
  [*
    this.IsInADomainChecked( false );
    this.IsBootNodeChecked( false );
    
    return FeedbackObject::IsAllowed();
  *]
}
