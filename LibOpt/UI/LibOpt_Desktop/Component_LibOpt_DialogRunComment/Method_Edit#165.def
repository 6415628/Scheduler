Quintiq file version 2.0
#parent: #root
Method Edit (GUIComponent parent, LibOpt_Run selection) id:Method_LibOpt_DialogRunComment_Edit_165
{
  #keys: '[139164.0.261240375]'
  Body:
  [*
    data := shadow( selection );
    
    Dialog.Data( data );
    
    if ( Dialog.DoModal( parent ) > 0 )
    {
      data.Commit();
    }
  *]
}
