Quintiq file version 2.0
#parent: #root
Method GetConfigurationErrors () declarative as owning Strings
{
  Description:
  [*
    Returns all errors of this object's configuration, each as a string in the returned struct.
    As this method may be overridden multiple times in a subtype ancestry line, don't forget to call `super.GetConfigurationErrors` at the beginning of the override body!
  *]
  TextBody: 'return construct( Strings );'
}
