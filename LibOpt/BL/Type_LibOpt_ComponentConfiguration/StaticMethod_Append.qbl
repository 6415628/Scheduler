Quintiq file version 2.0
#parent: #root
StaticMethod Append (output String string, String new) declarative remote
{
  TextBody:
  [*
    if( string <> '' )
    {
      string := string + String::NewLine();
    }
    
    string := string + new;
  *]
}
