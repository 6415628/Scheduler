Quintiq file version 2.0
#parent: #root
StaticMethod ParseTags (String string) const declarative remote as owning Strings
{
  TextBody:
  [*
    result := construct( Strings );
    
    LibOpt_Utility::ParseTags( string, result );
    
    return & result;
  *]
}
