Quintiq file version 2.0
#parent: #root
Method GetAnchorConstRemote (const LibOpt_Run run) const declarative remote as const LibOpt_Anchor
{
  Description: 'Get the `LibOpt_Anchor` object belonging to the `LibOpt_ScopeElement` and the given `LibOpt_Run`, if it exists.'
  TextBody: 'return select( this, Anchor, anchor, true, anchor.Run() = run );'
}
