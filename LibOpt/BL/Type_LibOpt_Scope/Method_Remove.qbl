Quintiq file version 2.0
#parent: #root
Method Remove (LibOpt_ScopeElements scope_elements)
{
  TextBody:
  [*
    traverse( scope_elements, Elements, se )
    {
      this.Remove( se );
    }
  *]
}
