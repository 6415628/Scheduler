Quintiq file version 2.0
#parent: #root
Method CreateRun (LibOpt_Optimizer optimizer, LibOpt_ScopeElements scope_elements) as LibOpt_Run
{
  Description: 'Create a new `LibOpt_Run` using the `LibOpt_Optimizer` passed in and the given `LibOpt_ScopeElements`.'
  TextBody:
  [*
    run := optimizer.CreateRun( scope_elements );
    
    optimizer.InitializeRun( run );
    
    return run;
  *]
}
