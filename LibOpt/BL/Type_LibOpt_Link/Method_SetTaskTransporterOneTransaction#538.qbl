Quintiq file version 2.0
#parent: #root
Method SetTaskTransporterOneTransaction (Boolean needs_propagation)
{
  Description:
  [*
    Sets a `LibOpt_TaskTransporterOneTransaction` to execute this link.
    As a result, the destination component will execute in the same transaction as the sender.
  *]
  TextBody: 'this.TaskTransporter( relnew, LibOpt_TaskTransporterOneTransaction, NeedsPropagation := needs_propagation );'
}
