Quintiq file version 2.0
#parent: #root
Method InitializeReactive (const LibOpt_Task task) const as stream[POAAlgorithm]
{
  Description: 'Initialize your `POAAlgorithm` here. You can spawn multiple transactions using Reactive Quill if required.'
  TextBody:
  [*
    // To be overridden. Initialize your POA here.
    // You can spawn multiple transactions using Reactive Quill if required.
    
    return this->Initialize( task );
  *]
}
