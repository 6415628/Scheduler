Quintiq file version 2.0
#parent: #root
Method HandleResult (POAAlgorithm poa, LibOpt_Task task) as LibOpt_Scope
{
  Description:
  [*
    Handle the result of the `POAAlgorithm` here.
    When overriding this, the provided rollback KPI will not get worse.
  *]
  TextBody:
  [*
    // To be overridden. Handle the result of the POA here.
    // When overriding this, the rollback KPI will not get worse.
    
    return task.Scope();
  *]
}
