Quintiq file version 2.0
#parent: #root
Method InitializeReactive (const LibOpt_Task task) const as stream[GraphProgram]
{
  Description: 'Initialize your `GraphProgram` here. You can spawn multiple transactions using Reactive Quill if required.'
  TextBody:
  [*
    // To be overridden. Initialize your graph program here.
    // You can spawn multiple transactions using Reactive Quill if required.
    
    return this->Initialize( task );
  *]
}
