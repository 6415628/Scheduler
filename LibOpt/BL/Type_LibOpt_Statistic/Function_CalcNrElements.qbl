Quintiq file version 2.0
#parent: #root
Function CalcNrElements
{
  Description: 'Calculate the number of elements (values / aspects / etc.) collected by this statistic.'
  TextBody:
  [*
    value := RealVector::Construct( this.ValuesAsRealVector() ).Size();
    
    this.NrElements( value );
  *]
}
