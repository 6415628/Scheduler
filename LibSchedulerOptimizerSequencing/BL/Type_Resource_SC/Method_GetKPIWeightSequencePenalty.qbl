Quintiq file version 2.0
#parent: #root
Method GetKPIWeightSequencePenalty (Real defaultpenalty) declarative remote as Real
{
  Description: 'Returns the KPI weight of the sequence penalty of this resource'
  TextBody:
  [*
    // By default return the default penalty
    // Can be overloaded on resource types to configure resource type dependent KPI weights
    return defaultpenalty;
  *]
}
