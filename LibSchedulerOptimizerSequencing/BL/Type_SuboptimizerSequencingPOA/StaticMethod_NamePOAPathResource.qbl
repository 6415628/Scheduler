Quintiq file version 2.0
#parent: #root
StaticMethod NamePOAPathResource (Resource_SC resource) const declarative remote as String
{
  Description: 'the name of the path (type) that represents the given resource'
  TextBody:
  [*
    value := resource.Name() + "/" + resource.ResourceID();
    return value;
  *]
}
