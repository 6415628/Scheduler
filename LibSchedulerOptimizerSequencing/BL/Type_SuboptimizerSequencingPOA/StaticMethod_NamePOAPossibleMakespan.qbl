Quintiq file version 2.0
#parent: #root
StaticMethod NamePOAPossibleMakespan () const declarative remote as String
{
  Description: 'the name of the POAPossibble that tells which operations are allowed on the makespan path (that only allows the makespan-end node)'
  TextBody:
  [*
    value := "makespan only";
    return value;
  *]
}
