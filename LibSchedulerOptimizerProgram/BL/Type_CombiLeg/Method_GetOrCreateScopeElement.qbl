Quintiq file version 2.0
#parent: #root
Method GetOrCreateScopeElement () as ScopeElementCombiLeg
{
  TextBody:
  [*
    // zyi1 Jul-8-2021 (created)
    result := this.ScopeElementCombiLeg();
    
    if( isnull( result ) )
    {
      result := this.ScopeElementCombiLeg( relnew );
    }
    
    return result;
  *]
}
