Quintiq file version 2.0
#parent: #root
MethodOverride GetTimeLimit (LibOpt_Run run) declarative as Duration
{
  TextBody:
  [*
    // the time limit is set independently of the run.
    return Duration::Zero();
  *]
}
