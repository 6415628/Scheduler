Quintiq file version 2.0
#parent: #root
Method FindOperation (internal[GUIComponent] parent, SingleTaskBase selection) id:Method_DialogFindOperationForSequence_FindOperation
{
  #keys: '[12910.0.463117774]'
  Body:
  [*
    /*selection.AutomationCalcOperationOnTask( true );
    this.Data( selection );
    num := this.DoModal( parent );
    if( num > 0 )
    {
      resource := selection.Resource_SC();
      resource.PlanBefore( DataHolderSelectedOperation.Data(), selection );
    }
    selection.AutomationCalcOperationOnTask( false );
    */
  *]
}
