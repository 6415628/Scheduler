Quintiq file version 2.0
#parent: #root
Method Edit (internal[GUIComponent] parent, POADirect selection) id:Method_DialogEditPOAParameters_Edit
{
  #keys: '[109706.1.1675638841]'
  Body:
  [*
    data := shadow( selection );
    
    Dialog.Data( data );
    
    if ( Dialog.DoModal( parent ) > 0 )
    {
       Dialog.Data().Commit();
    }
  *]
}
