Quintiq file version 2.0
#parent: #root
Method Edit (internal[GUIComponent] parent, SubTaskTypeOnResourceGroup selection) id:Method_DialogSubTaskTypeOnResourceGroup_Edit
{
  #keys: '[129856.0.58711394]'
  Body:
  [*
    data := shadow( selection );
    
    Dialog.Data( data );
    
    if ( Dialog.DoModal( parent ) > 0 )
    {
      data.Commit();
    }
  *]
}
