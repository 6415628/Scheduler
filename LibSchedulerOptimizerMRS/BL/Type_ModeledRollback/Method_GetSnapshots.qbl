Quintiq file version 2.0
#parent: #root
Method GetSnapshots (LibOpt_Task task) as owning SnapshotReplannableKPIs
{
  Description: 'returns all snapshots this modeled rollback component should consider rolling back to.'
  IsAbstract: true
  TextBody:
  [*
    // We expect this method to be overridden.
    result := construct( SnapshotReplannableKPIs );
    return & result;
  *]
}
