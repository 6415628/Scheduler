Quintiq file version 2.0
#parent: #root
MethodOverride ShouldRollback (const RealVector kpi_pre, const RealVector kpi_post) as Boolean
{
  TextBody:
  [*
    // mbr23, 2019-09-10 (overridden)
    
    return false; // ShouldRollback is used for rollbacks in Operation, not in OnFinalize. We have our own rollback logic in OnFinalize.
  *]
}
