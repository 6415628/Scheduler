Quintiq file version 2.0
#parent: #root
StaticMethod Create (LibOpt_Run owner, String name, RunContextSolvedConflicts runcontext) as ResetterRunContext
{
  TextBody:
  [*
    // mbr23 Sep-2-2019 (created)
    return owner.Component( relnew,
                            ResetterRunContext,
                            Name := name,
                            RunContextSolvedConflicts := runcontext );
  *]
}
