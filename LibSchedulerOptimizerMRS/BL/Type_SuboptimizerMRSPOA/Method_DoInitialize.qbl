Quintiq file version 2.0
#parent: #root
MethodOverride DoInitialize (LibOpt_Task task) as owning Algorithm
{
  TextBody:
  [*
    // Martijn: Overloading this method is ugly, but making our initialization const requires significant effort and gives very little benefit
    this.InitializeNonConst( task );
  *]
}
