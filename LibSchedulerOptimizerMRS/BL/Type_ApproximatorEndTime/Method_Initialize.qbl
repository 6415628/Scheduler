Quintiq file version 2.0
#parent: #root
MethodOverride Initialize (const LibOpt_Task task) const as owning MathematicalProgram
{
  TextBody:
  [*
    // We should not get here since we haven't implemented a constant initialization
    assert( false, "The const Initialize method of SubOptimizerApproximatorEndTime is called but this has not been implemented. Should use the InitializeNonConst instead");
    
    return null( MathematicalProgram, owning );
  *]
}
