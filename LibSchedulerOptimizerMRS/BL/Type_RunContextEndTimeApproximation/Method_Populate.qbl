Quintiq file version 2.0
#parent: #root
Method Populate (LibOpt_Scope scope)
{
  Description: "ensures that this run context has links to all resource groups of the given scope. If it doesn't, creates them."
  TextBody:
  [*
    // mbr23 Sep-16-2019 (created)
    this.Populate( scope.ResourceGroups() );
  *]
}
