Quintiq file version 2.0
#parent: #root
MethodOverride Update
{
  TextBody:
  [*
    // mbr23, 2019-09-13 (overridden)
    
    // instantiate owned subsettings if necessary:
    if( isnull( this.POA() ) )
    {
      this.POA( relnew );
    }
    if( isnull( this.Subiteration() ) )
    {
      this.Subiteration( relnew );
    }
    if( isnull( this.Meta() ) )
    {
      this.Meta( relnew );
    }
  *]
}
