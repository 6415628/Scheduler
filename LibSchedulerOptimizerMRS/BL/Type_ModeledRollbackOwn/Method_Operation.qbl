Quintiq file version 2.0
#parent: #root
MethodOverride Operation (LibOpt_Task task) as stream[JSON]
{
  TextBody:
  [*
    // mbr23, 2019-09-10 (overridden)
    
    // create a full replannable snapshot owned by this ComponentSnapshot:
    this.CapturePlan( task, task.SnapshotComponent() );
    
    return super.Operation( task );
  *]
}
