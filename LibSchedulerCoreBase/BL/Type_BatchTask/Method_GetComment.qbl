Quintiq file version 2.0
#parent: #root
MethodOverride GetComment () declarative remote as String
{
  TextBody:
  [*
    //Get comment for the batch
    return guard( this.Batch().Comment(), '' );
  *]
}
