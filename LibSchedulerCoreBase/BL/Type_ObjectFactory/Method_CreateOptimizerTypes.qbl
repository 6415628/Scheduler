Quintiq file version 2.0
#parent: #root
Method CreateOptimizerTypes
{
  TextBody:
  [*
    // mbr23, 2019-09-16: removed OptimizerMRS* types, as they are refactored to LibOpt.
    
    optimizer := this.Schedule().Optimizer();
    
    if( not isnull( optimizer ) )
    {
      // EXTEND this method and initialize your custom OptimizerTypes here if they still use the legacy Scheduler optimization framework (as opposed to Optimizer Components)
    }
  *]
}
