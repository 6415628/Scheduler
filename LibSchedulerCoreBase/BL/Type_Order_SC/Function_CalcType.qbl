Quintiq file version 2.0
#parent: #root
Function CalcType
{
  TextBody:
  [*
    // MODELING HANDLE SC
    // Order type if taken from product type. Project may want to change this.
    
    value := guard( this.Product_SC().Type(), '' );
    
    this.Type( value );
  *]
}
