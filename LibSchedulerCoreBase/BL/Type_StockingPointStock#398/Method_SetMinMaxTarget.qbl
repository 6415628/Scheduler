Quintiq file version 2.0
#parent: #root
Method SetMinMaxTarget (Real min_i, Real max_i, Real target_i)
{
  TextBody:
  [*
    this.MinimumStockLevel( min_i );
    this.MaximumStockLevel( max_i );
    this.TargetAmount( target_i );
  *]
}
