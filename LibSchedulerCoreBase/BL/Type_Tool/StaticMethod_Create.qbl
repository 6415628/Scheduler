Quintiq file version 2.0
#parent: #root
StaticMethod Create (ToolGroup owner, String name, Number index, 
  String toolid, String toolgrouptype) as Tool
{
  TextBody:
  [*
    tool := owner.Tool( relnew, Name := name, Index := index, ToolID := toolid, ToolGroupType := toolgrouptype );
    
    return tool
  *]
}
