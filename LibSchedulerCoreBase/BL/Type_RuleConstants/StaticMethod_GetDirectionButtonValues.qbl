Quintiq file version 2.0
#parent: #root
StaticMethod GetDirectionButtonValues () const declarative remote as owning Strings
{
  Description: 'Get direction button values'
  TextBody:
  [*
    // Get direction button values
    values := construct( Strings );
    
    values.Add( RuleConstant::Direction_Upwards() );
    values.Add( RuleConstant::Direction_Downwards() );
    values.Add( RuleConstant::Direction_UpwardsDownwards() );
    
    return &values;
  *]
}
