Quintiq file version 2.0
#parent: #root
Method UpdateTask (Operation_SC operation)
{
  TextBody:
  [*
    task := guard( operation.Task_SC(), null( Task_SC ) );
    this.Task_SC( relset, task );
  *]
}
