Quintiq file version 2.0
#parent: #root
Method UpdateTimeInterval (ExplicitTimeInterval interval, DateTime start, DateTime end)
{
  Description: 'Update start, end of time interval. Used when processing calendar feedback from MES system'
  TextBody:
  [*
    // jiex Sep-30-2016 (created)
    
    interval.Start( start );
    interval.End( end );
  *]
}
