Quintiq file version 2.0
#parent: #root
StaticMethod SubTaskDescription_NoBufferActionInput () const declarative remote as String
{
  TextBody:
  [*
    // wkn1 May-29-2017 (created)
    return 'NoBufferActionInput subtask is used when a resource needs to receive multiple materials from the upstream resource before it can start processing the task';
  *]
}
