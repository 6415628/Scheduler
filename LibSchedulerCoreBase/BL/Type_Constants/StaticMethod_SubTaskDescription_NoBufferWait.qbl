Quintiq file version 2.0
#parent: #root
StaticMethod SubTaskDescription_NoBufferWait () const declarative remote as String
{
  TextBody:
  [*
    // wkn1 May-29-2017 (created)
    return 'NoBufferWait subtask is used when a resource needs to hold the material until it can start processing at the downstream resource';
  *]
}
