Quintiq file version 2.0
#parent: #root
StaticMethod SubTaskDescription_Setup () const declarative remote as String
{
  TextBody:
  [*
    // wkn1 May-29-2017 (created)
    return 'Setup subtask is used to reserve resource time that is needed before the processing of a task can start';
  *]
}
