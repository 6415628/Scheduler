Quintiq file version 2.0
#parent: #root
StaticMethod SubTaskDescription_NoBufferAction () const declarative remote as String
{
  TextBody:
  [*
    // wkn1 May-29-2017 (created)
    return 'NoBufferAction subtask is used when a resource needs to hold multiple materials which takes from the first material - until the final material can start processing at the downstream resource';
  *]
}
