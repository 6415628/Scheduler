Quintiq file version 2.0
#parent: #root
Method CreateDemandInBatch (Demand demand) as owning DemandInBatch
{
  TextBody:
  [*
    return construct( GenericDemandInBatch,
                      Demand := demand,
                      DemandID := demand.DemandID() );
  *]
}
