Quintiq file version 2.0
#parent: #root
Method Unplan
{
  Description: 'Delete batch task'
  TextBody:
  [*
    if( not isnull( this.BatchTask() ) )
    {
      this.BatchTask().Unplan();
    }
  *]
}
