Quintiq file version 2.0
#parent: #root
Method RemoveOperation (Operation_SC operation)
{
  Description: 'Remove selected operation from batch'
  TextBody:
  [*
    if( operation.GetBatch() = this )
    {
      operation.RemoveFromBatch();
    }
  *]
}
