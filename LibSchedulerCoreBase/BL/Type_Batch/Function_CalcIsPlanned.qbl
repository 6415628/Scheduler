Quintiq file version 2.0
#parent: #root
Function CalcIsPlanned
{
  Description: 'Calculate if batch is planned by checking if task in a batch.'
  TextBody:
  [*
    value := not isnull( this.BatchTask() );
    
    this.IsPlanned( value );
  *]
}
