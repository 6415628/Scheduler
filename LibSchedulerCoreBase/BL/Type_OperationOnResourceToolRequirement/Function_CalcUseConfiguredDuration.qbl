Quintiq file version 2.0
#parent: #root
Function CalcUseConfiguredDuration
{
  TextBody:
  [*
    // WOI1 May-18-2016 (created)
    //error( this.DefinitionName() );
    value := this.ToolRequirementBase().ToolRequirementPattern().UseConfiguredDuration();
    
    this.UseConfiguredDuration( value );
  *]
}
