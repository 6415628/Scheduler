Quintiq file version 2.0
#parent: #root
Method GetHasValidReservationQuantity (output Strings feedback_o) declarative as Boolean
{
  Description: 'Check if imported reservation has a valid quantity, which must always be lesser than the supply or demand quantity.'
  TextBody:
  [*
    // to be overloaded in the subclasses because there are different supplies/demands used for different fixed reservations
    // checks if the quantity reserved is lesser than the supply or demand quantity, pass sanity check if lesser, fail if greater
    
    return true;
  *]
}
