Quintiq file version 2.0
#parent: #root
Function CalcIsCompleted
{
  TextBody:
  [*
    
    value := guard( this.ProgramType().TaskInProgramSet( relsize ) = 0, true );
    
    this.IsCompleted( value );
  *]
}
