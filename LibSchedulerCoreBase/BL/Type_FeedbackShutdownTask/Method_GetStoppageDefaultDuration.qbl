Quintiq file version 2.0
#parent: #root
MethodOverride GetStoppageDefaultDuration () declarative as Duration
{
  TextBody: 'return guard( this.Shutdown().ResponsibleShutdownType().DefaultDuration(), Duration::Zero() );'
}
