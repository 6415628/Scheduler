Quintiq file version 2.0
#parent: #root
MethodOverride UpdateComment (String comment)
{
  TextBody:
  [*
    //Update comment for operation
    if( not isnull( this.GetOperation() ) )
    {
      this.GetOperation().UpdateComment( comment ); 
    }
  *]
}
