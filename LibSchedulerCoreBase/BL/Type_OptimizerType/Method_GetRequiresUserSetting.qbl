Quintiq file version 2.0
#parent: #root
Method GetRequiresUserSetting () const declarative remote as Boolean
{
  Description: 'If the optimizer type should have an OptimizerUserSetting, used in the UI for blocking the planner to start an optimizer.'
  TextBody:
  [*
    // By default no UserSetting is required. Should be overridden for optimizer types which can be called by the user
    return false;
  *]
}
