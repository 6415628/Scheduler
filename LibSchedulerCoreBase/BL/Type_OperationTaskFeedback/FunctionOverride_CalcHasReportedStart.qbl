Quintiq file version 2.0
#parent: #root
FunctionOverride CalcHasReportedStart
{
  TextBody:
  [*
    value := guard( this.Last().HasReportedStart(),
                    false );
    
    this.HasReportedStart( value );
  *]
}
