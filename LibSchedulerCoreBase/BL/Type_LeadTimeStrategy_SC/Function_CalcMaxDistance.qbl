Quintiq file version 2.0
#parent: #root
Function CalcMaxDistance
{
  TextBody:
  [*
    // tvn1 Jan-5-2017 (created)
    
    value := guard( this.NextLeadTimeStrategy().MinDistance(), Duration::MaxDuration() );
    
    this.MaxDistance( value );
  *]
}
