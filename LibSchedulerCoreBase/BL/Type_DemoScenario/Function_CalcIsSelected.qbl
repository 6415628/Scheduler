Quintiq file version 2.0
#parent: #root
Function CalcIsSelected
{
  TextBody:
  [*
    value := this.Name() = this.DemoCategory().DemoManager().DemoDataSelected()
             and this.DemoCategory().Name() = this.DemoCategory().DemoManager().DemoCategorySelected();
    
    this.IsSelected( value );
  *]
}
