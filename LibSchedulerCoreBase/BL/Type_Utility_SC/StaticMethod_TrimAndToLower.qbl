Quintiq file version 2.0
#parent: #root
StaticMethod TrimAndToLower (String input) const declarative remote as String
{
  Description: 'Trims the input at both ends and then converts all characters to lowercase letters.'
  TextBody:
  [*
    value := input.TrimBoth().ToLower();
    
    return value;
  *]
}
