Quintiq file version 2.0
#parent: #root
Method GetMaterialPlannedSupplyId (String operationid) const as String
{
  TextBody:
  [*
    // WOI1 Jul-28-2016 (created)
    // WOI1 Jul-28-2016 (created)
    // similar to plan unit id but without the PU string.
    return 'M_' + operationid + '-' + this.Id();
  *]
}
