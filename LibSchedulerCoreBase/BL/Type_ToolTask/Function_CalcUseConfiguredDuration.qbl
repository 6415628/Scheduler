Quintiq file version 2.0
#parent: #root
Function CalcUseConfiguredDuration
{
  Description: 'Check whether the tool task need to operate as long as the duration specified by user.'
  TextBody:
  [*
    // alexandern Oct-30-2015 (created)
    
    value := this.OperationOnResourceToolRequirement().UseConfiguredDuration();
    
    this.UseConfiguredDuration( value );
  *]
}
