Quintiq file version 2.0
#parent: #root
Method Update (String name, Real lotSize, Real minSize, 
  Real maxSize)
{
  Description: 'Updates information on this instance.'
  TextBody:
  [*
    this.Name( name );
    this.LotSize( lotSize );
    this.MinSize( minSize );
    this.MaxSize( maxSize );
  *]
}
