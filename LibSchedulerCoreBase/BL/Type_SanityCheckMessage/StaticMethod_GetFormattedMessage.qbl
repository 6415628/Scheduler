Quintiq file version 2.0
#parent: #root
StaticMethod GetFormattedMessage (String instance, String message) const declarative remote as String
{
  Description: 'return the message with instance + feedback message'
  TextBody:
  [*
    // chin wai Jul-20-2016 (created)
    
    spacing := ': ';
    
    return instance + spacing + message;
  *]
}
