Quintiq file version 2.0
#parent: #root
Method GetBatch () declarative remote as Batch
{
  Description: 'Return batch for operation'
  TextBody:
  [*
    return guard( this.BatchSequenceElement().Batch(),
                  null( Batch ) );
  *]
}
