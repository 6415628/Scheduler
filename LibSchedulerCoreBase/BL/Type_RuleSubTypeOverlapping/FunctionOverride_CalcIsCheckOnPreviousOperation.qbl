Quintiq file version 2.0
#parent: #root
FunctionOverride CalcIsCheckOnPreviousOperation
{
  TextBody:
  [*
    // Overlapping need to check on previous operation
    this.IsCheckOnPreviousOperation( true );
  *]
}
