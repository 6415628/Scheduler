Quintiq file version 2.0
#parent: #root
FunctionOverride CalcIsMinimumCombinedLength
{
  TextBody:
  [*
    // Set to TRUE if is it minimum combined length
    value := this.IsMinimum();
    
    this.IsMinimumCombinedLength( value );
  *]
}
