Quintiq file version 2.0
#parent: #root
Method Update (ImportedStockingPoint_SC row)
{
  TextBody:
  [*
    this.Name( row.Name() );
    this.Index( row.Index() );
    this.ImportedStockingPoint_SC( relset, row );
  *]
}
