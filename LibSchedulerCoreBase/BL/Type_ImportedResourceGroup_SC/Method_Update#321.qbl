Quintiq file version 2.0
#parent: #root
Method Update (Boolean hasBatches, Boolean hasFixedSequencePlanLogic, Boolean hasPractice, 
  Boolean isContinuous)
{
  TextBody:
  [*
    this.HasBatches( hasBatches );
    this.HasFixedSequencePlanLogic( hasFixedSequencePlanLogic );
    this.HasPractice( hasPractice );
    this.IsContinuous( isContinuous );
  *]
}
