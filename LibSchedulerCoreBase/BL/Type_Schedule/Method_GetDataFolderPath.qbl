Quintiq file version 2.0
#parent: #root
Method GetDataFolderPath () const as String
{
  Description: 'To get the full path to the data folder where the excel files are stored.'
  TextBody: 'return FileSystems::QFSPathToHardwarePath( DomainModel::GetDataDirectory().Path() );'
}
