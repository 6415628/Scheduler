Quintiq file version 2.0
#parent: #root
Method DeleteBatches (Batches batches)
{
  Description: 'Delete all batches'
  TextBody:
  [*
    traverse( batches, Elements, batch )
    {
      batch.Delete()
    }
  *]
}
