Quintiq file version 2.0
#parent: #root
Function CalcCurrentTargetMinLevel
{
  TextBody:
  [*
    value := guard( this.CurrentInventoryTarget().MinimumStockLevel(), 0 );
    
    this.CurrentTargetMinLevel( value );
  *]
}
