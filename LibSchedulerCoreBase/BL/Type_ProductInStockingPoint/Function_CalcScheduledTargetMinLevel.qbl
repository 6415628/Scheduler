Quintiq file version 2.0
#parent: #root
Function CalcScheduledTargetMinLevel
{
  TextBody:
  [*
    value := guard( this.ScheduledInventoryTarget().MinimumStockLevel(), 0 );
    
    this.ScheduledTargetMinLevel( value );
  *]
}
