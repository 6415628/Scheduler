Quintiq file version 2.0
#parent: #root
Function CalcScheduledTargetLevel
{
  TextBody:
  [*
    value := guard( this.ScheduledInventoryTarget().TargetStockLevel(), 0 );
    
    this.ScheduledTargetLevel( value );
  *]
}
