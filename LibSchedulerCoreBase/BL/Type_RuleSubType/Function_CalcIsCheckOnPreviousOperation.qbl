Quintiq file version 2.0
#parent: #root
Function CalcIsCheckOnPreviousOperation
{
  TextBody:
  [*
    // TO BE OVERLOADED on each rule subtype which need to check on previous operation
    
    value := false;
    
    this.IsCheckOnPreviousOperation( value );
  *]
}
