Quintiq file version 2.0
#parent: #root
Method Update (CalendarPeriod period)
{
  TextBody:
  [*
    this.Factor( period.Factor() );
    this.From( period.From() );
    this.ResourceID( period.Calendar().astype( ResourceCalendar ).Resource_SC().ResourceID() );
    this.RuleGroupName( period.GroupName() );
  *]
}
