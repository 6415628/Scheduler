Quintiq file version 2.0
#parent: #root
StaticMethod NumberToStringUserConverter (Number value) const declarative remote as String
{
  TextBody:
  [*
    converter := NumberToString::UserConverter();
    return guard( converter.Convert( value ), '' );
  *]
  InterfaceProperties { Accessibility: 'Extensible' }
}
