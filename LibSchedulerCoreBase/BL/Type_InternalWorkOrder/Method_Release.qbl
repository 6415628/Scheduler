Quintiq file version 2.0
#parent: #root
Method Release
{
  Description: 'Sets internal work order IsReleased to follow IsMarkedForRelease, called right before creating ExportedInternalWorkOrders during publish. Only marked for release work orders can be released.'
  TextBody: 'this.IsReleased( this.IsMarkedForRelease() );'
}
