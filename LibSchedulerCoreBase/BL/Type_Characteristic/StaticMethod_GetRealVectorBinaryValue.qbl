Quintiq file version 2.0
#parent: #root
StaticMethod GetRealVectorBinaryValue (String realstring) declarative remote as BinaryValue
{
  Description: 'Get real vector binary value based on input string which concatenate the real values'
  TextBody:
  [*
    // Get real vector binary value based on input string which concatenate the real values
    
    return RealVector::Construct( realstring, Constants::SemiColon() ).AsBinaryValue();
  *]
}
