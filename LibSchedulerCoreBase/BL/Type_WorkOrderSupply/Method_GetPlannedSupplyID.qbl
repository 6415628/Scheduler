Quintiq file version 2.0
#parent: #root
Method GetPlannedSupplyID (String operationid) const as String
{
  Description: 'Generates a unique planned supply ID.'
  TextBody:
  [*
    // WOI1 Jul-28-2016 (created)
    // similar to plan unit id but without the PU string.
    return 'P_' + operationid + '-' + this.Id();
  *]
}
