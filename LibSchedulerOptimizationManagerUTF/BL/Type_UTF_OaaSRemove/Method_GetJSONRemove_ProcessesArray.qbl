Quintiq file version 2.0
#parent: #root
Method GetJSONRemove_ProcessesArray () const as owning JSONBuilder
{
  Description: 'Generates a standard complete JSON remove body call for processes.'
  TextBody:
  [*
    // Create shiftPatterns array
    processesArray := JSON::Array();
    
    // Add shiftPatternID to be removed
    processesArray.Add( "Process1" );
    
    return &processesArray;
  *]
  InterfaceProperties { Accessibility: 'Module' }
}
