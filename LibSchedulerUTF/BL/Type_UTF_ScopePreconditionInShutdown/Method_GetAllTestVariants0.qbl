Quintiq file version 2.0
#parent: #root
Method GetAllTestVariants () as owning Strings
{
  Description: 'Return all test variant names/IDs as structured string.'
  TextBody:
  [*
    hasCharacteristicScopes := construct( Booleans );
    thresholdValue := construct( Reals );
    isValids := construct( Booleans );
    
    return this.GetAllTestVariants( hasCharacteristicScopes,
                                    thresholdValue,
                                    isValids );
  *]
}
