Quintiq file version 2.0
#parent: #root
Method TestHandleBatchFeedback_BatchTaskReceiveStartStatus_Setup (Schedule schedule)
{
  TextBody:
  [*
    this.Setup( schedule );
    
    // Propagate all
    Transaction::Transaction().PropagateAll();
  *]
}
