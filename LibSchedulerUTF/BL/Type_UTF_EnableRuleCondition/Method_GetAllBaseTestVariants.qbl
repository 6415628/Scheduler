Quintiq file version 2.0
#parent: #root
Method GetAllBaseTestVariants () as owning Strings
{
  Description: 'Return all test variant names/IDs as structured string.'
  TextBody:
  [*
    variants := construct( Strings );
    
    return &variants;
  *]
}
