Quintiq file version 2.0
#parent: #root
Method GetAllTestVariants (output Strings conditionTypes, output Booleans conditionValues, output Booleans operationAttributeValues, 
  output Booleans hasNoViolateConstraints) as owning Strings
{
  Description: 'Return all test variants, variant output arguments (columns) will be used in data creation logic.'
  TextBody:
  [*
    variants := construct( Strings );
    
    return &variants;
  *]
}
