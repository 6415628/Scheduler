Quintiq file version 2.0
#parent: #root
Method Setup (Schedule schedule)
{
  Description: 'Standard setup logic for unit tests related to production environment configurations.'
  TextBody:
  [*
    this.CreateImportedData( schedule );
    this.SynchronizeImportedData( schedule );
  *]
}
