Quintiq file version 2.0
#parent: #root
StaticMethod CreateRuleScopeInProgramPhase (Rule rule, ProgramPhaseConfiguration programphase) as RuleScopeInProgramPhase
{
  Description: 'Create rule scope in program phase.'
  TextBody:
  [*
    // Create rule scope in single resource 
    ruleScopeInProgramPhase := rule.RuleScopeInProgramPhase( relnew );
    ruleScopeInProgramPhase.ProgramPhaseConfiguration( relset, programphase );
    
    return ruleScopeInProgramPhase;
  *]
}
