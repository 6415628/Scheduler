Quintiq file version 2.0
#parent: #root
FunctionOverride CalcStart
{
  TextBody:
  [*
    if( this.HasStartUTF() )
    {
      // Manipulate start time procedurally
      this.Start( this.Start_UTF() );
    }
    else
    {
     super.CalcStart();
    }
  *]
}
