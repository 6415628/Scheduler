Quintiq file version 2.0
#parent: #root
Method GetLayerVariants (output Strings characteristicTypes, output Reals ruleValues, output Booleans isMinimums, 
  output Reals operationAttributeValues, output Booleans hasNoConstraintViolations) as owning Strings
{
  Description: 'Return layer specific variants.'
  TextBody:
  [*
    // To be extended in layer
    
    return construct( Strings );
  *]
  InterfaceProperties { Accessibility: 'Extensible' }
}
