Quintiq file version 2.0
#parent: #root
MethodOverride DeleteCondition (LibOpt_Task task) as Boolean
{
  TextBody:
  [*
    error( "Throw an error to test if errors in the 'DeleteCondition' method are handled properly in 'DoFinalizeDatasetCopyDelete'." );
    
    return true;
  *]
}
