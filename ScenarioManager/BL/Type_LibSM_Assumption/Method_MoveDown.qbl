Quintiq file version 2.0
#parent: #root
Method MoveDown (LibSM_Assumption afterElement)
{
  Description: 'Move element down. Affecting display index for sorting.'
  TextBody:
  [*
    if( not isnull( afterElement ) )
    {
      this.Scenario().MoveAfterAssumption( this, afterElement );
    }
  *]
}
