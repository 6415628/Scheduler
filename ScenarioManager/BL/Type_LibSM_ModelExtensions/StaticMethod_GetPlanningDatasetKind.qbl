Quintiq file version 2.0
#parent: #root
StaticMethod GetPlanningDatasetKind () declarative remote as String
{
  Description:
  [*
    MUST EXTEND | Returns the kind of the planning dataset (excluding the MDS prefix). Eg. 'Company'. 
    Only extend if that is not 'Company'.
  *]
  TextBody: 'return "Company";'
  InterfaceProperties { Accessibility: 'Extensible' }
}
