Quintiq file version 2.0
#parent: #root
StaticMethod DefaultFrequencyDaemonClean () const remote as Duration
{
  Description: 'The default frequency to run the CleanUp daemon with (if not specified by the user).'
  TextBody:
  [*
    // Set the default interval to 6 hours.
    return Duration::Hours( 6 );
  *]
}
