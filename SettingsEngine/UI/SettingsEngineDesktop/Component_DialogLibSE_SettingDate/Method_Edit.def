Quintiq file version 2.0
#parent: #root
Method Edit (GUIComponent parent, LibSE_SettingDate selection) id:Method_DialogLibSE_SettingDate_Edit
{
  #keys: '[139164.3.587332287]'
  Body:
  [*
    data := shadow( selection );
    
    Dialog.Data( data );
    
    if ( Dialog.DoModal( parent ) > 0 )
    {
      data.Commit();
    }
  *]
}
