Quintiq file version 2.0
#parent: #root
Method Update (JSON parameter)
{
  Description: 'Update optimization service parameters based on input JSON.'
  TextBody:
  [*
    this.Strategy( ImportedOptimizationServiceParameter::GetStrategy( parameter ) ); 
    this.MaxDuration( ImportedOptimizationServiceParameter::GetMaxDuration( parameter ) );
  *]
}
