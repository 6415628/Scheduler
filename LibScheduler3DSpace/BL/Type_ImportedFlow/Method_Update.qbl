Quintiq file version 2.0
#parent: #root
Method Update (JSON flow)
{
  Description: 'Updates flow instance based on JSON input.'
  TextBody:
  [*
    this.StartOperationID( ImportedFlow::GetStartOperationID( flow ) ); 
    this.EndOperationID( ImportedFlow::GetEndOperationID( flow ) );
    this.DependencyType( ImportedFlow::GetDependencyType( flow ) );
  *]
}
