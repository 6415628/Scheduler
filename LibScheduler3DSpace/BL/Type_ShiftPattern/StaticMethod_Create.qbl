Quintiq file version 2.0
#parent: #root
StaticMethod Create (Schedule owner, String id) as ShiftPattern
{
  Description: 'Creates a shift pattern (calendar) that holds all the time periods (events) to be subscribed by resource calendars.'
  TextBody: 'return owner.ShiftPattern( relnew, ShiftPattern, CalendarID := id );'
}
