Quintiq file version 2.0
#parent: #root
Method Update (JSON process)
{
  Description: 'Update process data based on received JSON from 3DSpace'
  TextBody:
  [*
    this.Name( ImportedRouting_SC::GetName( process ) );
    this.Label( ImportedRouting_SC::GetLabel( process ) );
  *]
}
