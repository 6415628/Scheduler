Quintiq file version 2.0
#parent: #root
Method Test052_ResourceExchangability_Positive (Schedule schedule)
{
  Description:
  [*
    Runs the SelectorSpan->Suboptimizer components of the optimizer and asserts that the optimizer has correctly identified that resource groups are exchangable.
    Input are three operations, one per type.
  *]
  TextBody:
  [*
    // precondition: the UTS KT must correcly set 3 scope operations
    this.Run_Test_ResourceExchangability_Positive( schedule, this.GetCurrentMethodShortName() );
  *]
}
