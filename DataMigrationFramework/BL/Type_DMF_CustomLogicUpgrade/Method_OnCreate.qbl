Quintiq file version 2.0
#parent: #root
MethodOverride OnCreate
{
  TextBody:
  [*
    // Create a single upgrade topic
    topic := DMF_Topic::FindCreateTopic( this.ConvertorDataset(), false );
    
    this.Topic( relset, topic );
  *]
}
