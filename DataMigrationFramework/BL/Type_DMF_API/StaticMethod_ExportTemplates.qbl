Quintiq file version 2.0
#parent: #root
StaticMethod ExportTemplates (DMF_DatasetTypes datasetType_i) as stream[Void]
{
  Description:
  [*
    Used by the UI to export a subset of core data schema (templates) after a **refresh** action is performed.  
    The user should not need to invoke this unless they wish to use a script to export their templates.
  *]
  TextBody:
  [*
    // tgn3 Apr-21-2021 (created)
    
    return DMF_API::ExportTemplates( Key::ZeroKey(), datasetType_i );
  *]
}
