Quintiq file version 2.0
#parent: #root
StaticMethod ExportCoreData (DMF_Datasets datasets_i) as stream[Void]
{
  Description:
  [*
    Used by the **execute** flow to export a subset of **core data** with the UI while DMF is running in export mode.  
    This API is not expected to be invoked by the end-user, as it is a default behavior of the **ExportMain** step.
  *]
  TextBody: 'return DMF_API::ExportCoreData( Key::ZeroKey(), datasets_i );'
}
