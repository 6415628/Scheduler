Quintiq file version 2.0
#parent: #root
Method SetPath
{
  Description: 'Set the path of the API, which will be used as the first part of the full EndpointPath of the API-calls.'
  IsAbstract: true
  TextBody:
  [*
    // Must be overridden to set the path of the API.
    // NOTE: the path must start with '/', and must not end with '/'.
    
    /* example
    path := "/resources/v1/dswoapi";
    this.Path( path );
    */
  *]
}
