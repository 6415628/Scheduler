Quintiq file version 2.0
#parent: #root
Method ParseObjectIDs (String json_i) as owning Strings
{
  Description: 'Parse the JSON that contains the IDs of the objects that must be retrieved.'
  TextBody:
  [*
    LibDIF_Util::MustBeOverridden( "ParseObjectIDs()", this.DefinitionName() );
    
    return null( Strings, owning );
  *]
}
