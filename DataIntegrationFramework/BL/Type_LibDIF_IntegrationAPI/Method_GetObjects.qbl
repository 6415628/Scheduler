Quintiq file version 2.0
#parent: #root
Method GetObjects (String dataTransformationID_i) as owning structured_Object
{
  Description: 'Get the objects that should be transformed into integration objects, based on the provided DataTransformationID.'
  TextBody:
  [*
    dataTransformation := this.GetDataTransformation( dataTransformationID_i );
    
    return dataTransformation.GetObjects();
  *]
}
