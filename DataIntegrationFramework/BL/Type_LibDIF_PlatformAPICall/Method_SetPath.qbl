Quintiq file version 2.0
#parent: #root
Method SetPath
{
  Description: 'Set the path of the API-method that will be called, which will be used as the second part of the full EndpointPath of the API-call.'
  IsAbstract: true
  TextBody:
  [*
    // Must be overridden
    // NOTE: the path must start with '/', and must not end with '/'.
    
    /* example
    path := "/invoke/dswo:searchWorkOrders";
    this.Path( path );
    */
  *]
}
