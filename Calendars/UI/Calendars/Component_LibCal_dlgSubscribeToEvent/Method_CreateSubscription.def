Quintiq file version 2.0
#parent: #root
Method CreateSubscription (LibCal_LeadingParticipation subscribeTo_i) id:Method_LibCal_dlgSubscribeToEvent_CreateSubscription
{
  #keys: '[131094.1.101639330]'
  Body:
  [*
    dlg := construct( LibCal_dlgSubscription );
    dlg.CreateSubscription( this, dhSubscribers.Data(), subscribeTo_i );
  *]
}
