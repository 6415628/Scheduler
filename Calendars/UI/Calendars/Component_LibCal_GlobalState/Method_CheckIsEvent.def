Quintiq file version 2.0
#parent: #root
Method CheckIsEvent (LibCal_Participation participation_i) as Boolean id:Method_LibCal_GlobalState_CheckIsEvent
{
  #keys: '[146730.0.129952510]'
  Body:
  [*
    // Check if an event is concerned by checking if participation_i is a LeadingParticipation.
    asEvent := true;
    return LibCal_Participation::CheckType( participation_i, asEvent );
  *]
}
