Quintiq file version 2.0
#parent: #root
Method CreateSubscription (GUIComponent parent_i, LibCal_Calendar calendar_i) id:Method_LibCal_GlobalState_CreateSubscription
{
  #keys: '[146730.0.344837808]'
  Body:
  [*
    // Create a subscription for a calendar, i.e. subscribe calendar_i to an event of another calendar.
    dlg := construct( LibCal_dlgSubscribeToEvent );
    dlg.Open( parent_i, calendar_i );
  *]
}
