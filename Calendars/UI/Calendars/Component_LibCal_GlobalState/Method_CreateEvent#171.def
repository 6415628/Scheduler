Quintiq file version 2.0
#parent: #root
Method CreateEvent (GUIComponent parent_i, LibCal_Calendar owner_i) as LibCal_Event id:Method_LibCal_GlobalState_CreateEvent_171
{
  #keys: '[146730.0.344832461]'
  Body:
  [*
    // Create a new Event.
    // The time of the planning-dataset will be suggested as start, and the general category will be used.
    dlg := construct( LibCal_dlgEvent );
    return dlg.CreateEvent( parent_i, owner_i );
  *]
}
