Quintiq file version 2.0
#parent: vhEndOfWindow
Response OnValueChanged () id:Response_LibCal_gcResourceCalendars_vhEndOfWindow_OnValueChanged
{
  #keys: '[131094.1.981127250]'
  Body:
  [*
    // Visualize the end of the calendar window with a ChartMarker.
    // Note that the window of the CommonCalendar is visualized, not the window of the individual resource calendars.
    // That is not possible using a ChartMarker, and the windows are typically synchronized.
    mrkEndOfWindow.SetProperty( "MarkPosition", this.Data().AsQUILL() );
  *]
  CanBindMultiple: false
  DefinitionID: 'Responsedef_ValueHolder_OnValueChanged'
}
