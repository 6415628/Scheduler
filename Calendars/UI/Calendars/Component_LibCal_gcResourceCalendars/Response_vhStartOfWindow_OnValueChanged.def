Quintiq file version 2.0
#parent: vhStartOfWindow
Response OnValueChanged () id:Response_LibCal_gcResourceCalendars_vhStartOfWindow_OnValueChanged
{
  #keys: '[131094.1.981126881]'
  Body:
  [*
    // Visualize the start of the calendar window with a ChartMarker.
    // Note that the window of the CommonCalendar is visualized, not the window of the individual resource calendars.
    // That is not possible using a ChartMarker, and the windows are typically synchronized.
    mrkStartOfWindow.SetProperty( "MarkPosition", this.Data().AsQUILL() );
  *]
  CanBindMultiple: false
  DefinitionID: 'Responsedef_ValueHolder_OnValueChanged'
}
