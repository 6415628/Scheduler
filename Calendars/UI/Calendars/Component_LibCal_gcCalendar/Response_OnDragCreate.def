Quintiq file version 2.0
#parent: #root
Response OnDragCreate (LibCal_Event row) id:Response_LibCal_gcCalendar_OnDragCreate
{
  #keys: '[131094.0.606338062]'
  Body:
  [*
    // Create a calendar event (participation).
    /* Commented out because there was argued that the drag-action is not intuitive here because it results
    // in a new row of the ganttchart instead of a new node on a row.
    LibCal_GlobalState.DetermineStartAndEndOfEvent( start, end );
    
    LibCal_GlobalState.CreateEvent( this, row.Calendar(), start, end );
  *]
  CanBindMultiple: false
  DefinitionID: 'Responsedef_GanttChart_OnDragCreate'
}
