Quintiq file version 2.0
#parent: #root
Method CreateSubscription (GUIComponent parent_i, LibCal_Calendar subscriber_i, LibCal_LeadingParticipation subscribeTo_i) id:Method_LibCal_dlgSubscription_CreateSubscription_602
{
  #keys: '[146730.0.713543444]'
  Body:
  [*
    subscribers := construct( LibCal_Calendars );
    subscribers.Add( subscriber_i );
    
    this.CreateSubscription( parent_i, subscribers, subscribeTo_i );
  *]
}
