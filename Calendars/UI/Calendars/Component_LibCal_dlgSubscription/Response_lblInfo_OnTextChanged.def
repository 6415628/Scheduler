Quintiq file version 2.0
#parent: lblInfo
Response OnTextChanged () id:Response_LibCal_dlgSubscription_lblInfo_OnTextChanged
{
  #keys: '[146730.0.1388541944]'
  Body:
  [*
    // Show the information about the recurrence of the Event, as tooltip of imgInfo.
    // Use a Label that is bound to the Info so that the text is updated when the Event is edited
    // (which can be triggered from dlgSubscription).
    imgInfo.SetProperty( "Tooltip", this.Text() );
  *]
  CanBindMultiple: false
  DefinitionID: 'Responsedef_Label_OnTextChanged'
}
