Quintiq file version 2.0
#parent: #root
Method CreateEvent (LibCal_Calendar owner_i) id:Method_LibCal_GlobalState_CreateEvent_30
{
  #keys: '[146730.0.344901497]'
  Body:
  [*
    // Create a new Event.
    // The time of the planning-dataset will be suggested as start, and the general category will be used.
    dlg := construct( LibCal_dlgEvent );
    dlg.CreateEvent( owner_i );
  *]
}
