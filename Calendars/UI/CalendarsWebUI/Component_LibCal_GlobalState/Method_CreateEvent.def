Quintiq file version 2.0
#parent: #root
Method CreateEvent (LibCal_Calendar owner_i, DateTime start_i, DateTime end_i) id:Method_LibCal_GlobalState_CreateEvent
{
  #keys: '[146730.0.344901485]'
  Body:
  [*
    // Create a new Event.
    // No EventCategory is provided, this will result in the general category being used.
    dlg := construct( LibCal_dlgEvent );
    dlg.CreateEvent( owner_i, start_i, end_i );
  *]
}
