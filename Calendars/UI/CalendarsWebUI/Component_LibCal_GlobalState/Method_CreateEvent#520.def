Quintiq file version 2.0
#parent: #root
Method CreateEvent (LibCal_Calendar owner_i, LibCal_EventCategory category_i) id:Method_LibCal_GlobalState_CreateEvent_520
{
  #keys: '[146730.0.344901494]'
  Body:
  [*
    // Create a new Event.
    // The time of the planning-dataset will be suggested as start.
    dlg := construct( LibCal_dlgEvent );
    dlg.CreateEvent( owner_i, category_i );
  *]
}
