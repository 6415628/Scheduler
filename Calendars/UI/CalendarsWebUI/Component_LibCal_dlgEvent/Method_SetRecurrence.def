Quintiq file version 2.0
#parent: #root
Method SetRecurrence (Boolean isRecurring_i) id:Method_LibCal_dlgEvent_SetRecurrence
{
  #keys: '[131094.1.9151575]'
  Body:
  [*
    if( isRecurring_i )
    {
      this.EnableRecurrence();
    }
    else
    {
      // Enable the dates for editing.
      this.EnableEvent();
      this.DisableRecurrence();
    }
  *]
}
