Quintiq file version 2.0
#parent: #root
Method CreateSubscription (LibCal_dlgSubscribeToEvent parent_i, structured[LibCal_Calendar] subscribers_i, LibCal_LeadingParticipation subscribeTo_i) id:Method_LibCal_dlgSubscription_CreateSubscription_545
{
  #keys: '[131094.1.564232359]'
  Body:
  [*
    // Register the dialog from where this dialog was opened, so it can be closed when OK is clicked.
    dhParent.Data( parent_i );
    
    this.CreateSubscription( subscribers_i, subscribeTo_i );
  *]
}
