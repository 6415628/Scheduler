Quintiq file version 2.0
#parent: #root
Method CreateSubscription (LibCal_dlgSubscribeToEvent parent_i, structured[LibCal_Calendar] subscribers_i, structured[LibCal_LeadingParticipation] subscribeTo_i) id:Method_LibCal_dlgSubscription_CreateSubscription_679
{
  #keys: '[146730.0.238471271]'
  Body:
  [*
    // Register the dialog from where this dialog was opened, so it can be closed when OK is clicked.
    dhParent.Data( parent_i );
    
    this.CreateSubscription( subscribers_i, subscribeTo_i );
  *]
}
