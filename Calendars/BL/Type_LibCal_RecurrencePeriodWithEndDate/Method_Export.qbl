Quintiq file version 2.0
#parent: #root
MethodOverride Export (LibCal_StagingParticipation stagingParticipation_i)
{
  TextBody:
  [*
    // Export generic attributes.
    super.Export( stagingParticipation_i );
    
    // Export specific attributes.
    stagingParticipation_i.PeriodEndDate( this.EndDateSet() );
  *]
}
