Quintiq file version 2.0
#parent: #root
StaticMethod DEFAULT_EXPORT_FILENAME () const remote as String
{
  Description:
  [*
    Default filename that will be used when exporting data.
    The user can change the filename when exporting, if allowed by the browser settings.
  *]
  TextBody: 'return "Calendars.xlsx";'
  InterfaceProperties { Accessibility: 'Extensible' }
}
