Quintiq file version 2.0
#parent: #root
Method SynchronizeData (LibCal_ImportExportDataset dataset_i, Boolean doDelete_i, Boolean keepIfNotImported_i)
{
  TextBody:
  [*
    // Trigger the synchronization of the calendars.
    // No specific context.
    dataset_i.SynchronizeData( this, doDelete_i, keepIfNotImported_i );
  *]
}
