Quintiq file version 2.0
#parent: #root
Method OnCreate
{
  TextBody:
  [*
    // Register the ImportExport-instance.
    // GetCalendarRegistry() must be overridden to get the model's CalendarRegistry.
    this.CalendarRegistry( relset, this.GetCalendarRegistry() );
  *]
  InterfaceProperties { Accessibility: 'Module' }
}
