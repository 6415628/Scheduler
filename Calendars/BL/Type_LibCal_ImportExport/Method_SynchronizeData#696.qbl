Quintiq file version 2.0
#parent: #root
Method SynchronizeData (LibCal_ImportExportDataset dataset_i, String context_i)
{
  TextBody:
  [*
    // Trigger the synchronization of the calendars.
    // Assume that a full sync is done, i.e. that the imported data is the complete new situation.
    dataset_i.SynchronizeData( this, context_i );
  *]
}
