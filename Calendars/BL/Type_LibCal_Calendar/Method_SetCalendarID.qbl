Quintiq file version 2.0
#parent: #root
Method SetCalendarID
{
  Description:
  [*
    Set the ID of the calendar. The CalendarID will be shown in the UI, so it should be human readable.
    The CalendarID must be unique per CalendarType (i.e. per subtype of LibCal_Calendar).
  *]
  IsAbstract: true
  TextBody:
  [*
    // Typically a non-technical ID from the object that owns the calendar.
    // Example: id := this.Machine().Name();
    
    id := "";
    
    this.CalendarID( id );
  *]
}
