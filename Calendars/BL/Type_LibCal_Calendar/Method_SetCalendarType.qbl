Quintiq file version 2.0
#parent: #root
Method SetCalendarType
{
  Description: 'CalendarType used for import/export.'
  TextBody:
  [*
    // By default, use the name of the subtype of LibCal_Calendar as CalendarType.
    // Override if necessary, for example use a less 'technical' name.
    calendarType := this.DefinitionName();
    
    this.CalendarType( calendarType );
  *]
}
