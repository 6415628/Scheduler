Quintiq file version 2.0
#parent: #root
Method Register
{
  TextBody:
  [*
    // Register the calendar.
    // GetCalendarRegistry() must be overridden for each type of calendar to get the model's CalendarRegistry.
    this.CalendarRegistry( relset, this.GetCalendarRegistry() );
  *]
}
