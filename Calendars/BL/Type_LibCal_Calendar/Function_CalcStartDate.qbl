Quintiq file version 2.0
#parent: #root
Function CalcStartDate
{
  TextBody:
  [*
    timezone := this.GetTimeZone();
    date     := this.Start().Date( timezone );
    
    this.StartDate( date );
  *]
  InterfaceProperties { Accessibility: 'Module' }
}
