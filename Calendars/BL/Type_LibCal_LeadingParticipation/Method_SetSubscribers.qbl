Quintiq file version 2.0
#parent: #root
Method SetSubscribers (LibCal_Calendars subscribers_i)
{
  TextBody:
  [*
    // For *new* subscribers, by default use the recurrence period of the LeadingParticipation of the event.
    useLeadingPeriod := true;
    
    this.SetSubscribers( subscribers_i, useLeadingPeriod );
  *]
  InterfaceProperties { Accessibility: 'Module' }
}
