Quintiq file version 2.0
#parent: #root
Method SetStart (Duration startTimeOfDay_i) as LibCal_Event
{
  Description:
  [*
    Set the start of the event.
    This moves the event, i.e. the end of the event is changed accordingly.
  *]
  TextBody:
  [*
    // Make the API fluent.
    return this.SetStart( this.LeadingParticipation().StartDate(), startTimeOfDay_i );
  *]
}
