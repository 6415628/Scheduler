Quintiq file version 2.0
#parent: #root
Method RemoveSubscriber (LibCal_Calendar subscriber_i)
{
  Description: 'Remove subscriber_i  as subscriber from the event.'
  TextBody:
  [*
    // Remove subscriber_i  as subscriber from the event by removing it as subscriber from its LeadingParticipation.
    this.LeadingParticipation().RemoveSubscriber( subscriber_i );
  *]
}
