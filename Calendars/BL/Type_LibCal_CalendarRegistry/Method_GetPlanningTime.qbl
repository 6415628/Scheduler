Quintiq file version 2.0
#parent: #root
Method GetPlanningTime () declarative as DateTime
{
  Description: 'Get the current time from the planning-dataset.'
  TextBody:
  [*
    // Overridden to take over the current time of the planning-dataset.
    // Example: planningTime := this.Company().Time();
    
    planningTime := DateTime::MinDateTime();
    
    return planningTime;
  *]
}
