Quintiq file version 2.0
#parent: #root
StaticMethod Event_Duration (Duration duration_i) as String
{
  TextBody:
  [*
    msg := "";
    
    if( duration_i < Duration::Zero() )
    {
      msg := "Duration must be >= 0:00";
    }
    
    return msg;
  *]
  InterfaceProperties { Accessibility: 'Module' }
}
