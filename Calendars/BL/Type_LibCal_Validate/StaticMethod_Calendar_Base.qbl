Quintiq file version 2.0
#parent: #root
StaticMethod Calendar_Base (DateTime base_i) as String
{
  TextBody:
  [*
    msg := "";
    
    if( base_i.IsMaxInfinity() )
    {
      msg := "Base is not allowed to be MaxDateTime";
    }
    
    return msg;
  *]
  InterfaceProperties { Accessibility: 'Module' }
}
