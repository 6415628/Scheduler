Quintiq file version 2.0
#parent: #root
StaticMethod Event_Pattern_Weekday (Number weekday_i) as String
{
  TextBody:
  [*
    msg := "";
    
    if( weekday_i < 1 or
        weekday_i > 7 )
    {
      msg := "Weekday must be >= 1 and <= 7";
    }
    
    return msg;
  *]
  InterfaceProperties { Accessibility: 'Module' }
}
