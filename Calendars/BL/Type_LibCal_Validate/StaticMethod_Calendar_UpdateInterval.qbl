Quintiq file version 2.0
#parent: #root
StaticMethod Calendar_UpdateInterval (Duration interval_i) as String
{
  TextBody:
  [*
    msg := "";
    
    if( interval_i < Duration::Zero() )
    {
      msg := "UpdateInterval must be >= 0:00";
    }
    
    return msg;
  *]
  InterfaceProperties { Accessibility: 'Module' }
}
